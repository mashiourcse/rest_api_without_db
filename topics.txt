MVC architecture

routes demo
    * /users => GET => {id,username,email}
    * /users => POST => {username, email}
    * /users/:id => PUT => update the username
    * /users/:id => DELETE => delete the user